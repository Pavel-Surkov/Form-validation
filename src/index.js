import { CustomValidation } from "./custom-validation";
//import { Validation } from "./validation";
import ReactDOM from "react-dom";

ReactDOM.render(<CustomValidation />, document.querySelector('#root'));
//ReactDOM.render(<Validation />, document.querySelector('#root'));